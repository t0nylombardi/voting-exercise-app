import * as React from "react";

function VotePage(): React.ReactElement {
  return (
    <div>
      <h1 className="text-2xl font-bold">Vote Page</h1>
      {/* TODO: Show candidate list + voting UI */}
    </div>
  );
}

export default VotePage;
